#!/bin/bash -e
cat <<EOF
This is Apache running PHP with OCI and SQL Server drivers S2I image:

s2i build <source> cetres/php-sql-oci_sti sample-app

You can then run the resulting image via:
docker run -p 8080:8080 sample-app

EOF
